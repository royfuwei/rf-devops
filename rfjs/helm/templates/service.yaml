apiVersion: v1
kind: Service
metadata:
  name: {{ include "rfjs.fullname" . }}
  labels:
    {{- include "rfjs.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
      nodePort: {{ .Values.service.externalPort }}
  selector:
    {{- include "rfjs.selectorLabels" . | nindent 4 }}
