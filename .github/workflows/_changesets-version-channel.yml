name: Changesets Version (by channel)

on:
  workflow_call:
    inputs:
      ref:
        type: string
        required: true
      channel:
        type: string
        required: true   # stable | alpha | beta | rc
      node_version:
        type: string
        required: false
        default: 20.x
      git_identity_mode:
        type: string
        required: false
        default: bot
      git_bot_name:
        type: string
        required: false
        default: github-actions[bot]
      git_bot_email:
        type: string
        required: false
        default: 41898282+github-actions[bot]@users.noreply.github.com

    secrets:
      GIT_TOKEN:
        required: true

jobs:
  version:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GIT_TOKEN }}
          ref: ${{ inputs.ref }}

      - name: Git identity
        shell: bash
        run: |
          set -euo pipefail
          if [ "${{ inputs.git_identity_mode }}" = "bot" ]; then
            git config --global user.name "${{ inputs.git_bot_name }}"
            git config --global user.email "${{ inputs.git_bot_email }}"
          else
            git config --global user.name "${GITHUB_ACTOR}"
            git config --global user.email "${GITHUB_ACTOR}@users.noreply.github.com"
          fi

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          run_install: false

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ inputs.node_version }}
          cache: pnpm

      - name: Install deps
        run: pnpm install --frozen-lockfile

      - name: Ensure changesets baseBranch exists
        shell: bash
        run: |
          set -euo pipefail
          BASE=$(node -p "require('./.changeset/config.json').baseBranch || 'main'")
          echo "Changesets baseBranch: $BASE"
          git fetch origin "+refs/heads/${BASE}:refs/remotes/origin/${BASE}"
          git branch -f "$BASE" "refs/remotes/origin/${BASE}"
          
          if git rev-parse --verify "$BASE" >/dev/null 2>&1; then
            echo "✅ Local branch '$BASE' is ready."
          else
            echo "❌ baseBranch '$BASE' not found"
            exit 1
          fi

      - name: Enter/Exit pre mode
        shell: bash
        run: |
          set -euo pipefail
          CHANNEL="${{ inputs.channel }}"
          
          if [ "$CHANNEL" = "stable" ]; then
            if [ -f ".changeset/pre.json" ]; then
              echo "Exiting pre-release mode..."
              npx changeset pre exit
            fi
          else
            if [ -f ".changeset/pre.json" ]; then
              CUR=$(node -p "require('./.changeset/pre.json').tag")
              if [ "$CUR" != "$CHANNEL" ]; then
                echo "Switching pre-release tag: $CUR -> $CHANNEL"
                npx changeset pre exit
                npx changeset pre enter "$CHANNEL"
              else
                echo "Already in pre-release mode ($CHANNEL)"
              fi
            else
              echo "Entering pre-release mode: $CHANNEL"
              npx changeset pre enter "$CHANNEL"
            fi
          fi

      # ✅ 強化版的偵測邏輯：結合 changeset status 與 git diff
      - name: Compute release plan
        id: plan
        shell: bash
        run: |
          set -euo pipefail
          
          # 1. 檢查是否有 .changeset/*.md 檔案 (Standard Changesets)
          # 排除 README.md 和 .changeset 內的其他設定檔
          CHANGESET_FILES=$(ls .changeset/*.md 2>/dev/null | grep -v 'README.md' || echo "")
          
          # 2. 同時執行 status 作為備援檢查
          npx changeset status --output changeset-status.json
          HAS_RELEASES_BY_STATUS=$(node -p "JSON.parse(require('fs').readFileSync('changeset-status.json')).releases.length > 0" 2>/dev/null || echo "false")
          
          if [ -n "$CHANGESET_FILES" ] || [ "$HAS_RELEASES_BY_STATUS" = "true" ]; then
            echo "has_releases=true" >> "$GITHUB_OUTPUT"
            echo "✅ Changes detected via status or changeset files."
          else
            echo "has_releases=false" >> "$GITHUB_OUTPUT"
            echo "ℹ️ No pending changesets found."
          fi

      - name: Apply versions
        if: steps.plan.outputs.has_releases == 'true'
        run: npx changeset version

      - name: Commit & push
        if: steps.plan.outputs.has_releases == 'true'
        shell: bash
        run: |
          set -euo pipefail
          git add -A
          # 只有在真的有變動時才 commit，避免 workflow 報錯
          if git diff --staged --quiet; then
            echo "No changes to commit."
          else
            git commit -m "chore(release): version packages (${{ inputs.channel }})"
            git push origin "${{ inputs.ref }}"
          fi

      - name: Open PR back to main
        if: steps.plan.outputs.has_releases == 'true'
        env:
          GH_TOKEN: ${{ secrets.GIT_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          FROM="${{ inputs.ref }}"
          TO="main"
          TITLE="chore(release): sync ${FROM} (${{ inputs.channel }}) back to ${TO}"
          BODY="Automated PR to sync versions and changelogs from ${FROM} to ${TO}."
          
          if gh pr list --base "$TO" --head "$FROM" --state open --json number -q 'length' | grep -q '^0$'; then
            gh pr create --base "$TO" --head "$FROM" --title "$TITLE" --body "$BODY"
          else
            echo "PR already exists: ${FROM} -> ${TO}"
          fi